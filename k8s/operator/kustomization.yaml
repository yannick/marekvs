apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - crd.yaml
  - rbac.yaml
  - deployment.yaml
  # the example cluster is applied separately:
  #   kubectl apply -f k8s/operator/example-cluster.yaml
