apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: marekvs
  labels:
    app: marekvs
spec:
  serviceName: marekvs-headless
  replicas: 3 # never scale below MAREKVS_REPLICAS_N + 1 (see README)
  podManagementPolicy: Parallel # join/leave ordering is gossip's job, not k8s's
  selector:
    matchLabels:
      app: marekvs
  template:
    metadata:
      labels:
        app: marekvs
    spec:
      # SIGTERM → Leaving phase → last ring entries flush → exit (~3s);
      # the generous budget covers slow gossip propagation under load.
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534 # image is FROM scratch; only /data must be writable
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: marekvs
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: marekvs
      containers:
        - name: marekvs
          # Published automatically by CI (.github/workflows/docker.yml)
          # on every push to main; pin a sha-/vX.Y.Z tag for production.
          image: ghcr.io/yannick/marekvs:latest
          ports:
            - containerPort: 6379
              name: resp
            - containerPort: 7373
              name: mesh
            - containerPort: 7946
              name: gossip
              protocol: UDP
            - containerPort: 9121
              name: metrics
          env:
            # NodeId comes from the pod hostname ordinal (marekvs-3 → 3);
            # no MAREKVS_NODE_ID needed in a StatefulSet.
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            # chitchat re-resolves this DNS name, so one seed entry covers
            # every pod behind the headless service.
            - name: MAREKVS_SEEDS
              value: "marekvs-headless.$(NAMESPACE).svc.cluster.local:7946"
            - name: MAREKVS_ADVERTISE_IP
              value: "auto" # self-detects the pod IP
            - name: MAREKVS_REPLICAS_N
              value: "2" # replication factor; every partition on 2 nodes
            - name: MAREKVS_DATA_DIR
              value: /data
            # Zone-aware placement (T2-11), opt-in and multi-zone only.
            #
            # MAREKVS_ZONE is this node's failure domain, gossiped so peers can
            # spread a partition's replicas across zones. NOTE: the downward
            # API can only read the POD's own labels, and
            # topology.kubernetes.io/zone lives on the NODE — there is no
            # fieldRef for it. Supply it one of these ways:
            #   * one StatefulSet per zone, each with a literal MAREKVS_ZONE
            #     and a nodeSelector pinning it there (simplest, no RBAC);
            #   * an initContainer with get-node RBAC that reads the node's
            #     label and writes it to a shared env file.
            # Leaving it unset is safe: placement degrades to plain HRW.
            #
            # MAREKVS_ZONE_AWARE turns the spreading on and MUST be identical
            # on every node — a mismatch splits ownership, and nodes log
            # PLACEMENT CONFIG MISMATCH when they detect one. Enabling it on a
            # live cluster reshuffles ownership, so treat the flip as a
            # maintenance operation and watch
            # marekvs_cluster_underreplicated_partitions drain.
            # - name: MAREKVS_ZONE
            #   value: "eu-central-1a"
            # - name: MAREKVS_ZONE_AWARE
            #   value: "1"
            - name: RUST_LOG
              value: info
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: "1"
              memory: 1Gi
            limits:
              memory: 1Gi # request = limit: no burst-OOM surprises
              # no CPU limit: throttling hurts tail latency
          lifecycle:
            preStop:
              httpGet:
                port: metrics
                path: /drain # phase → Leaving before SIGTERM lands
          startupProbe:
            httpGet:
              port: metrics
              path: /ready
            failureThreshold: 180 # up to 6 min for long bootstraps
            periodSeconds: 2
          readinessProbe:
            httpGet:
              port: metrics
              path: /ready # 200 while Active|Leaving (serves during drain)
            periodSeconds: 2
          livenessProbe:
            httpGet:
              port: metrics
              path: /alive # process + shard threads responsive
            initialDelaySeconds: 10
            periodSeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: data
        labels:
          app: marekvs
      spec:
        accessModes: [ReadWriteOnce]
        resources:
          requests:
            storage: 10Gi
        # storageClassName: local-nvme  # prefer local PVs for performance
